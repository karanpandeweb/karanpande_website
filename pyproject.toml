[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi", "uvicorn", "python-dotenv", "psycopg[binary,pool]", "pydantic", "pyjwt", "cryptography", "python-multipart", "requests" ]
requires-python = "~=3.12.0"
